# Configuracion de Auth del proyecto freetools, en un fichero para que no viva
# solo en el panel.
#
# QUE ES ESTO Y POR QUE ESTA AQUI
# El "Send Email Hook" de Supabase (lo que hace que el codigo de acceso salga por
# Brevo con nuestra marca) se activa desde el panel a mano. Un ajuste que solo
# vive en un panel es un ajuste que nadie sabe que existe hasta que se rompe. Con
# este fichero se puede volver a dejar el proyecto como esta hoy con un comando.
#
# COMO SE APLICA
#   cd <una carpeta con supabase/config.toml que sea este fichero>
#   export SUPABASE_ACCESS_TOKEN=...
#   export SEND_EMAIL_HOOK_SECRET='v1,whsec_...'
#   supabase config push --project-ref wabnolojhxlqhevehybw
#
# ⚠️ `config push` MANDA LA CONFIGURACION ENTERA DE AUTH, no solo lo que pongas
# aqui: lo que no escribas se queda con el valor por defecto de la CLI, no con el
# que tenga hoy el proyecto. El 2026-09-06 eso apago el MFA por TOTP y bajo el
# limite de correos a uno por segundo sin que nadie lo pidiera. Por eso estan
# escritos abajo los valores que importan, aunque coincidan con el defecto.
#
# LOS OTROS SECRETOS DE freetools, que no van aqui porque son secretos:
#   GEMINI_API_KEY          la clave del modelo
#   BREVO_API_KEY           para mandar el correo del codigo
#   PLANNING_AI_HASH_SALT   la sal de la huella del cliente
#   SEND_EMAIL_HOOK_SECRET  el que firma el hook, el mismo que el de abajo
#   PLANNING_AI_ORIGENES    las webs desde las que se acepta llamar a la IA;
#                           hoy el dominio propio, el de Vercel y localhost
#
# ⚠️ El secreto NO va en este fichero. Va en la variable de entorno de arriba, y
# tiene que ser el MISMO que el secreto `SEND_EMAIL_HOOK_SECRET` de las Edge
# Functions, o la funcion rechaza todo con 401.

project_id = "freetools"

[functions.planning-ai]
verify_jwt = false

[functions.planning-auth-email]
verify_jwt = false

[auth]
# La direccion de verdad del planificador. Si no esta aqui, alguien entra con su
# codigo y la sesion NO vuelve: Auth solo devuelve a las direcciones de esta lista.
#
# Estan las cuatro que existen: el dominio propio, el que da Vercel (que sigue
# funcionando y se usa para mirar un despliegue antes de apuntar el DNS), y las
# dos de local. Los despliegues de vista previa de Vercel llevan una direccion
# distinta cada vez, asi que ahi el login no vuelve: es a proposito, no se ponen
# comodines sobre vercel.app porque eso abre el retorno a cualquier proyecto.
site_url = "https://planificador.shifty.es"
additional_redirect_urls = [
  "https://planificador.shifty.es",
  "https://shifty-planning.vercel.app",
  "http://localhost:5174",
  "http://127.0.0.1:5174",
]

[auth.email]
enable_signup = true
# ENCENDIDO desde el 2026-09-07, y NO rompe el codigo de seis digitos: se probo
# con un usuario nuevo de punta a punta despues de cambiarlo.
#
# Estaba apagado, y eso dejaba la puerta abierta a quedarse con la cuenta de
# otro: cualquiera podia darse de alta con el correo de un restaurante y una
# contrasena suya, y /auth/v1/signup devolvia sesion EN EL ACTO con el correo
# marcado como verificado sin que nadie hubiera probado nada. Comprobado contra
# freetools antes de arreglarlo.
#
# Con esto encendido, esa alta ya no entrega sesion: el atacante se queda fuera
# con un "Email not confirmed".
#
# LO QUE ESTO NO ARREGLABA, y estaba medido: si la victima entraba DESPUES con
# su codigo, su correo quedaba confirmado y la contrasena que dejo puesta el
# atacante volvia a valer. Eso lo cierra el disparador de
# backend/sql/30-nada-de-contrasenas.sql, que impide guardar ninguna contrasena.
# Esto se queda encendido igualmente: es una capa mas y no cuesta nada.
enable_confirmations = true
otp_length = 6
otp_expiry = 3600
# Un correo por minuto y direccion. Sin esto, cualquiera puede llenarle el buzon a
# otro pulsando el boton, y ademas se come la cuota de Brevo.
max_frequency = "60s"

# ⚠️ ESTOS LIMITES SON POR HORA Y PARA TODO EL PROYECTO, no por persona.
# Venian a 30, y eso significa que con 30 personas entrando en una hora la
# herramienta deja de mandar codigos PARA TODOS. Para un lead magnet que se
# ensena en una feria o que sale en un correo, 30 se agotan en diez minutos.
# Se subieron el 2026-09-07 despues de comerselos en las propias pruebas.
#
# El techo de verdad no es este, es Brevo: su plan gratuito tiene un tope diario
# propio. Si empieza a fallar el envio con la cuota de Supabase de sobra, mirar
# ahi.
[auth.rate_limit]
email_sent = 150
sign_in_sign_ups = 500
token_verifications = 500

# Apagado. Se probo para rechazar las altas con contrasena y NO SIRVE: el aviso
# que manda es identico para un alta con contrasena y para una con codigo, asi
# que no se puede rechazar una sin rechazar la otra. Lo que cierra ese agujero es
# el disparador de backend/sql/30-nada-de-contrasenas.sql.
[auth.hook.before_user_created]
enabled = false
uri = ""
secrets = ""

# No lo usa el planificador (se entra con un codigo por correo), pero estaba
# encendido antes de tocar nada y apagarlo no era parte del encargo.
[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

[auth.hook.send_email]
enabled = true
uri = "https://wabnolojhxlqhevehybw.supabase.co/functions/v1/planning-auth-email"
secrets = "env(SEND_EMAIL_HOOK_SECRET)"
